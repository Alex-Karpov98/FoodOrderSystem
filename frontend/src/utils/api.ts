// api utility
// Handles API calls to backend services

export async function fetchMenuItems() {
  // Call backend to get menu items
}

export async function createOrder(orderData: any) {
  // Send selected cart data to backend to create order
}

export async function processPayment(paymentData: any) {
  // Send payment request to backend with card info
}
